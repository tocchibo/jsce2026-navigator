[project]
name = "conference-navigator"
version = "0.1.0"
description = "JSCE/JSME 2026 conference navigator data tools"
requires-python = ">=3.11"
dependencies = [
  "pymupdf>=1.26,<2",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
  "websockets>=15,<16",
]
