## 1. 対話

- ユーザーとの対話は必ず **日本語** で行うこと。

## 2. 環境・パッケージ管理

- Python の環境とパッケージ管理は **必ず `uv` を使うこと**。
- グローバル環境を一切変更してはならない。
  - `pip install` / `conda install` / `python -m venv` などを直接使わない。
  - `apt` / `brew` など OS のパッケージ管理コマンドも使わない。
- 実行例や手順を書くときも、`uv run` や `uv add` など `uv` のコマンドだけを示すこと。

## 3. 実装ポリシー

- 仕様・論文・設計の内容を読み、それに **決定的に従う実装** を行うこと。
- 仕様がよく分からない部分を、場当たり的な `if` 文や推測で処理してはならない。
  - 分からないケースは「対応しない」としてエラーにしてよい。

## 4. アプリの実行環境

- 実行PCのスペック：
 - マザーボード: MSI MAG X570S TOMAHAWK MAX WIFI (X570 AM4 ATX)
 - CPU: AMD Ryzen 9 5900X
 - メモリ: 128GB (DDR4)
 - GPU: 
    - メイン: Nvidia RTX5070ti 16GB (PCIe 4.0 x16, CPU直結)
    - サブ: Nvidia RTX4060ti 16GB  (Oculink使用PCIe 4.0 x4, X570経由)
 - CUDA UMD Version: 13.3
- windows 11 (日本語)での実行を前提とする。
- 環境に最も適した文字コードを用いることに注意する。
- powershellでの文字化けにも注意し、適切な対策を行う。
